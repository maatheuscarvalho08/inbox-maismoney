# PRODUÇÃO: aponte para o domínio publicado, ex. https://seu-dominio.com/api
# (o Nginx do deploy/nginx.conf já espera /api sob o mesmo domínio do frontend).
# Esse valor é lido em build time (npm run build) — se mudar, rode o build de novo.
VITE_API_URL=http://localhost:3333/api
